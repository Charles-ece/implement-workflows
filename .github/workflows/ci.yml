name: CI
on: [push, pull_request]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: cmake --build build
      - uses: actions/upload-artifact@v4
        with:
          { name: bin, path: build/cpu_monitor_app }  
  test:
    runs-on: self-hosted 
    needs: build
    steps:
      - uses: actions/download-artifact@v4
      - run: make test
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - run: ./deploy.sh